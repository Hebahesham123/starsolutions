import { NotFoundBody } from '@/components/NotFoundBody';

/** notFound() from an Arabic page. The /ar layout supplies the chrome. */
export default function ArabicNotFound() {
  return <NotFoundBody locale="ar" />;
}
